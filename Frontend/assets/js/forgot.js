
(() => {
  const form = document.getElementById("forgot-form");
  form?.addEventListener("submit", (e) => {
    e.preventDefault();
    alert("Se envió un enlace para recuperar tu contraseña (simulado)");
  });
})();
